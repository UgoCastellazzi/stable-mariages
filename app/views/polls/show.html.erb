<h1><%= @poll.name %></h1>

<ul>
  <% @poll.receivers.each do |receiver| %>
    <li><%= receiver.name %> <%= receiver.capacity%> <%= link_to "edit", edit_receiver_path(receiver) %>
      <%= link_to "delete",
            receiver_path(receiver),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
      </li>
  <% end %>
  <p><%= @poll.respondents.length %> respondents</p>
</ul>

<p>Créer un receiver</p>
<%= simple_form_for [ @poll, @receiver ] do |f| %>
  <%= f.input :name %>
  <%= f.input :capacity %>
  <%= f.submit "Create receiver", class: "btn btn-primary" %>
<% end %>

<%= link_to "Partager le lien d'inscription", new_poll_respondent_path(@poll) %>